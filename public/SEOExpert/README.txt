SEOExpert — Black-Only Package (v2, clean symbol crop)

Included:
- Full logo (black), transparent PNG (8000px wide), 1000 DPI metadata
- Symbol-only (black), transparent PNG (4000px wide) — cleaned to remove any wordmark remnants
- Wordmark-only (black), transparent PNG (8000px wide)
- Icon set (symbol-only): 32–1024px PNG
- SVGs: embedded-PNG SVG files for full logo and symbol

If you need a true editable vector SVG (paths), tell me and I’ll trace it (may slightly simplify brush edges).
